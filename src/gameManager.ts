import { MapLoader } from "./mapLoader";

export class GameManager {
    public static MapLoader: MapLoader;
    public static Scale: number = 3;
    public static BaseWidth: number = 256;
    public static BaseHeight: number = 192;
}